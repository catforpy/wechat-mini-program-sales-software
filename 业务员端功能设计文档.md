# 业务员端功能设计文档

## 📋 目录

1. [功能概述](#功能概述)
2. [页面设计](#页面设计)
3. [我的排名功能](#我的排名功能)
4. [好评展示功能](#好评展示功能)
5. [API接口需求](#api接口需求)
6. [前端实现要点](#前端实现要点)

---

## 功能概述

业务员端是业务员使用的专属客户端，主要功能包括：

### 1. 核心功能
- ✅ 我的销售业绩
- ✅ 我的排名展示
- ✅ 客户管理
- ✅ 好评管理
- ✅ 消息通知
- ✅ 数据分析

### 2. 角色权限
- 只能查看自己的数据
- 可以查看自己的排名
- 可以管理自己的客户
- 可以回复客户评价

---

## 页面设计

### 1. 业务员端首页

```
┌─────────────────────────────────────┐
│  业务员工作台                        │
├─────────────────────────────────────┤
│  👋 早上好，小张                     │
│  今日销售：3单  💰¥24,000           │
├─────────────────────────────────────┤
│  📊 我的排名                         │
│  本周排名：第1位  ↑ 2               │
│  团队排名：3/50                      │
│  [查看详情]                          │
├─────────────────────────────────────┤
│  📈 销售趋势                         │
│  [折线图]                            │
├─────────────────────────────────────┤
│  ⭐ 最新好评                         │
│  "服务很专业！" - 某某商户           │
│  [更多好评]                          │
├─────────────────────────────────────┤
│  快捷入口                            │
│  [我的客户] [销售记录] [评价管理]     │
└─────────────────────────────────────┘
```

### 2. 我的排名详情页

```
┌─────────────────────────────────────┐
│  ← 我的排名                          │
├─────────────────────────────────────┤
│  🏆 当前排名：第1位                  │
│  👤 小张                             │
│  ↑ 上升2位 （上周第3位）              │
├─────────────────────────────────────┤
│  📊 销售数据                         │
│  • 销售数量：15单                    │
│  • 销售金额：¥128,000                │
│  • 绑定商户：8个                     │
│  • 好评率：4.8⭐                     │
├─────────────────────────────────────┤
│  📈 历史排名曲线                     │
│  [图表显示最近30天排名变化]           │
├─────────────────────────────────────┤
│  🎯 目标完成度                       │
│  本月目标：¥200,000                  │
│  已完成：64% ████████░░              │
│  还差：¥72,000                       │
└─────────────────────────────────────┘
```

### 3. 好评管理页

```
┌─────────────────────────────────────┐
│  ← 我的好评                          │
├─────────────────────────────────────┤
│  [全部好评] [5星] [4星]              │
├─────────────────────────────────────┤
│  ⭐⭐⭐⭐⭐ 5.0                      │
│  "小张服务非常专业，响应速度快！"     │
│  - 某某商城  2026-02-03             │
│  [回复] [点赞:12]                    │
├─────────────────────────────────────┤
│  ⭐⭐⭐⭐⭐ 5.0                      │
│  "模板质量很好，售后也很贴心"         │
│  - 某某教育  2026-02-02             │
│  [回复] [点赞:8]                     │
├─────────────────────────────────────┤
│  统计数据                            │
│  • 总评价数：48条                    │
│  • 好评率：95.8%                     │
│  • 5星评价：42条                     │
│  • 4星评价：4条                      │
│  • 平均分：4.8⭐                     │
└─────────────────────────────────────┘
```

---

## 我的排名功能

### 功能描述

在业务员登录后的首页，显著展示"我的排名"信息，让业务员随时了解自己在团队中的排名情况。

### 数据结构

```typescript
interface MyRankingData {
  // 当前排名
  currentRank: number

  // 排名变化趋势
  trend: {
    direction: 'up' | 'down' | 'stable'  // 上升/下降/稳定
    value: number                         // 变化位数
    previousRank: number                  // 上次排名
    icon: string                          // 趋势图标
    text: string                          // 趋势文本
  }

  // 团队信息
  teamInfo: {
    totalMembers: number    // 团队总人数
    myPosition: number      // 我的位置
    topRanker: string       // 第一名是谁
  }

  // 销售数据
  salesData: {
    salesCount: number      // 销售数量
    salesAmount: number     // 销售金额
    merchantCount: number   // 商户数量
    rating: number          // 好评率
  }

  // 历史排名（用于绘制曲线图）
  rankHistory: Array<{
    date: string      // 日期
    rank: number      // 当天排名
  }>

  // 目标完成度
  target: {
    monthlyGoal: number        // 月度目标
    completedAmount: number    // 已完成金额
    completionRate: number     // 完成百分比
    remainingAmount: number    // 剩余金额
  }
}
```

### API接口

**接口地址：** `GET /api/salesperson/my-ranking`

**请求头：**
```
Authorization: Bearer {token}
```

**请求参数：**

| 参数名 | 类型 | 必填 | 说明 |
|--------|------|------|------|
| timeRange | string | 否 | 时间范围 today/week/month/year，默认week |

**响应示例：**

```json
{
  "code": 200,
  "message": "success",
  "data": {
    "currentRank": 1,
    "trend": {
      "direction": "up",
      "value": 2,
      "previousRank": 3,
      "icon": "↑",
      "text": "上升2位"
    },
    "teamInfo": {
      "totalMembers": 50,
      "myPosition": 1,
      "topRanker": "小张（就是你）"
    },
    "salesData": {
      "salesCount": 15,
      "salesAmount": 128000.00,
      "merchantCount": 8,
      "rating": 4.8
    },
    "rankHistory": [
      { "date": "2026-02-01", "rank": 5 },
      { "date": "2026-02-02", "rank": 3 },
      { "date": "2026-02-03", "rank": 2 },
      { "date": "2026-02-04", "rank": 1 },
      { "date": "2026-02-05", "rank": 1 },
      { "date": "2026-02-06", "rank": 1 },
      { "date": "2026-02-07", "rank": 1 }
    ],
    "target": {
      "monthlyGoal": 200000,
      "completedAmount": 128000,
      "completionRate": 64,
      "remainingAmount": 72000
    }
  }
}
```

### 前端实现示例

```vue
<template>
  <view class="my-ranking-card">
    <view class="ranking-header">
      <text class="title">📊 我的排名</text>
      <text class="view-detail" @click="goToDetail">查看详情 ›</text>
    </view>

    <view class="ranking-content">
      <!-- 当前排名 -->
      <view class="current-rank">
        <text class="rank-number">第{{ myRanking.currentRank }}位</text>
        <view class="trend-badge" :class="myRanking.trend.direction">
          <text class="trend-icon">{{ myRanking.trend.icon }}</text>
          <text class="trend-text">{{ myRanking.trend.text }}</text>
        </view>
      </view>

      <!-- 团队信息 -->
      <view class="team-info">
        <text class="info-text">团队排名：{{ myRanking.myPosition }}/{{ myRanking.totalMembers }}</text>
      </view>

      <!-- 销售数据 -->
      <view class="sales-grid">
        <view class="sales-item">
          <text class="sales-value">{{ myRanking.salesData.salesCount }}单</text>
          <text class="sales-label">销售数量</text>
        </view>
        <view class="sales-item">
          <text class="sales-value">¥{{ formatAmount(myRanking.salesData.salesAmount) }}</text>
          <text class="sales-label">销售金额</text>
        </view>
        <view class="sales-item">
          <text class="sales-value">{{ myRanking.salesData.merchantCount }}个</text>
          <text class="sales-label">绑定商户</text>
        </view>
        <view class="sales-item">
          <text class="sales-value">{{ myRanking.salesData.rating }}⭐</text>
          <text class="sales-label">好评率</text>
        </view>
      </view>
    </view>
  </view>
</template>

<script setup lang="ts">
import { ref, onMounted } from 'vue'

const myRanking = ref<MyRankingData | null>(null)

const loadMyRanking = async () => {
  const res = await uni.request({
    url: '/api/salesperson/my-ranking',
    method: 'GET'
  })
  myRanking.value = res.data.data
}

const goToDetail = () => {
  uni.navigateTo({ url: '/src/pages/salesperson/ranking-detail' })
}

onMounted(() => {
  loadMyRanking()
})
</script>
```

---

## 好评展示功能

### 功能描述

在业务员端首页展示收到的客户好评，可以选择查看特定业务员或特定模板的好评。

### 1. 好评轮播卡片

```vue
<template>
  <view class="reviews-carousel">
    <view class="carousel-header">
      <text class="title">⭐ 客户好评</text>
      <view class="selector">
        <picker
          :range="reviewOptions"
          :value="selectedReviewIndex"
          @change="handleReviewChange"
        >
          <view class="picker-value">
            {{ reviewOptions[selectedReviewIndex] }} ▼
          </view>
        </picker>
      </view>
    </view>

    <swiper
      class="review-swiper"
      :autoplay="true"
      :interval="5000"
      :circular="true"
    >
      <swiper-item v-for="(review, index) in reviews" :key="index">
        <view class="review-card">
          <view class="rating-stars">
            <text v-for="i in 5" :key="i">⭐</text>
          </view>
          <text class="review-text">{{ review.comment }}</text>
          <view class="review-footer">
            <text class="merchant-name">- {{ review.merchantName }}</text>
            <text class="review-date">{{ review.createdAt }}</text>
          </view>
        </view>
      </swiper-item>
    </swiper>
  </view>
</template>
```

### 2. 好评筛选选项

```javascript
const reviewOptions = [
  '我的好评',
  '团队好评',
  '培训模板好评',
  '电商模板好评',
  '点餐模板好评'
]

const selectedReviewIndex = ref(0)

const handleReviewChange = (e) => {
  selectedReviewIndex.value = e.detail.value
  loadReviews()
}
```

### API接口

**接口地址：** `GET /api/reviews/carousel`

**请求参数：**

| 参数名 | 类型 | 必填 | 说明 |
|--------|------|------|------|
| type | string | 否 | 类型 my/team/template |
| targetId | int | 否 | 目标ID（type为template时必填） |
| limit | int | 否 | 返回数量，默认10 |

**响应示例：**

```json
{
  "code": 200,
  "message": "success",
  "data": {
    "reviews": [
      {
        "id": 1,
        "merchantName": "某某商城",
        "merchantAvatar": "https://example.com/avatar.jpg",
        "rating": 5.0,
        "comment": "小张服务非常专业，响应速度快，强烈推荐！",
        "salespersonName": "小张",
        "templateName": "电商商城模板",
        "createdAt": "2026-02-03",
        "likes": 12
      }
    ]
  }
}
```

---

## API接口需求

### 1. 获取我的排名

```
GET /api/salesperson/my-ranking
```

### 2. 获取我的销售数据

```
GET /api/salesperson/my-sales
```

### 3. 获取我的好评

```
GET /api/salesperson/my-reviews
```

### 4. 回复评价

```
POST /api/reviews/reply
{
  "reviewId": 1,
  "reply": "感谢您的评价！"
}
```

### 5. 获取客户列表

```
GET /api/salesperson/my-merchants
```

---

## 前端实现要点

### 1. 图表展示

使用 uCharts 或 ECharts 展示数据可视化：

```javascript
// 排名趋势图
const rankChart = {
  type: 'line',
  data: {
    categories: myRanking.value.rankHistory.map(item => item.date),
    series: [{
      name: '排名',
      data: myRanking.value.rankHistory.map(item => item.rank)
    }]
  },
  xAxis: { type: 'category' },
  yAxis: { type: 'value', inverse: true }  // 排名越小越好，所以反转Y轴
}

// 目标完成度（环形进度条）
const targetChart = {
  type: 'gauge',
  data: [{
    value: myRanking.value.target.completionRate
  }]
}
```

### 2. 实时更新

```javascript
// 定时刷新排名数据（每5分钟）
onMounted(() => {
  loadMyRanking()
  const timer = setInterval(loadMyRanking, 5 * 60 * 1000)

  onUnmounted(() => {
    clearInterval(timer)
  })
})
```

### 3. 数据缓存

```javascript
// 使用本地缓存减少API请求
const loadMyRanking = async () => {
  const cacheKey = 'my_ranking_week'
  const cached = uni.getStorageSync(cacheKey)

  if (cached && Date.now() - cached.timestamp < 60000) {
    myRanking.value = cached.data
    return
  }

  const res = await api.getMyRanking()
  myRanking.value = res.data

  uni.setStorageSync(cacheKey, {
    data: res.data,
    timestamp: Date.now()
  })
}
```

---

## 开发优先级

### P0（必须有）
- ✅ 我的排名展示
- ✅ 销售数据统计
- ✅ 好评列表

### P1（重要）
- 排名趋势图
- 目标完成度
- 好评轮播

### P2（可选）
- 数据对比功能
- 历史数据导出
- 业绩预测

---

**文档维护者：** 开发团队
**最后更新：** 2026-02-08
**版本：** 1.0.0
